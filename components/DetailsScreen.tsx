import React from 'react';
import { View, Text } from 'react-native';

const DetailsScreen: React.FC = () => {
  return (
    <View>
      <Text className="text-xl font-bold text-blue-500">Details Screen</Text>
    </View>
  );
};

export default DetailsScreen;
